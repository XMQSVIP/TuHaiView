# 文件名搜索验证（2026-09-06）

本文保留初版搜索构建的测试与哈希。后续已加入扫描顺序稳定修复：同一查询下新匹配项追加到末尾，扫描完成后统一排序；主动搜索或切换排序立即生效。当前构建和最新回归见 [扫描顺序修复报告](SCAN-ORDER.md)，下方历史数据不转记到新 EXE。

## 构建与自动测试

- `cargo fmt -- --check`、`git diff --check`、`cargo check --locked --message-format=short` 通过。
- `cargo test --locked --all-targets -- --test-threads=1`：54 项通过、6 项按原有/手工基准条件忽略，无失败。另行执行了新增的 Release 搜索基准。
- `cargo build --release --locked` 通过，未更新版本号、发布或推送。
- 最终 EXE SHA-256：`8E08AA331AE196B99B7960E137201B590FA62ED6D04486FBC94E2C8DD5105873`。

新增回归覆盖名称匹配、防抖、查询版本回退、中文组词确认/取消/焦点丢失、搜索框快捷键、四种排序与去重组合、同名文件位于不同目录、扫描快照替换及切换根目录时的迟到结果。

## 5 万条记录后台基准

命令：`cargo test --release --locked sorting::tests::search_50k_latency -- --ignored --exact --nocapture --test-threads=1`。

每类 20 次，包含实际等待的 150 ms 防抖及后台排序结果接收；采用文件名自然排序，无图片磁盘访问或 GPU 操作。

| 场景 | P50 | P95 | 最大值 |
| --- | ---: | ---: | ---: |
| 大量匹配（50,000） | 212.88 ms | 257.09 ms | 282.66 ms |
| 少量匹配（1） | 173.93 ms | 211.69 ms | 214.97 ms |
| 零匹配 | 183.09 ms | 227.51 ms | 231.73 ms |

三类 P95 均满足 ≤300 ms 目标。此基准在最终全局快捷键修正前执行；之后后台搜索、搜索状态及基准代码未变。它不包含 UI 调度与屏幕呈现。

## 实际窗口验证

使用独立测试 EXE、数据库和 37 张合成图片；原始图片目录未参与文件操作。通过 Windows 辅助功能树和键盘验证了：

- Ctrl+F 聚焦并选中文字；中文查询、英文忽略大小写、部分名称和零结果提示。
- 搜索框内 Ctrl+A/Delete 只编辑文字，Esc 清空后恢复列表，再次 Esc 退出焦点。
- 全选 37 张后筛到 10 张，已选数量降为 10；清空后显示 37 张，选择仍为 10。
- 删除确认仅列出这 10 张测试图片；没有确认删除，测试文件仍为 37 张。
- 通过应用自带渲染截图检查了搜索工具栏与网格布局。

最终 Release EXE 另行打开完整的 `C:\tuhai-fixtures-20260905\catalog` 合成图库，界面确认已收录 50,000 张、扫描完成。依次查询 `image`、`49999`、`no-match`，分别显示 50,000、1、0 张；`search_apply_ms` 为 **185.09 / 181.64 / 163.54 ms**（每类一次，仅作整窗冒烟验证，不作为 P95）。

本机原始日志：`G:\tuhai-search-qa-20260906\logs-50k\performance-1788690994187284400-11604.jsonl`。退出凭据：complete，accepted=181219，dropped=0。日志不记录搜索词。

## 验证边界

Windows.Graphics.Capture 返回 `SetIsBorderRequired ... 0x80004002`，鼠标自动化无法获取输入几何信息，因此没有完成真实窗口的鼠标预览翻页、滚动流畅度、窄窗口和拖拽回归。中文 IME 使用真实 egui 输入事件做自动测试，未使用系统候选窗口逐项手测。筛选后的文件操作检查到确认范围，未在 UI 中执行删除或复制/移动。

应用内耗时包含防抖和结果应用，不包含 OS 输入队列到物理屏幕呈现的完整延迟；没有使用上述数据宣称物理输入到显示达标。测试启动的产品进程均已关闭，测试 EXE、合成图片、日志留在独立 QA 目录便于复查。
