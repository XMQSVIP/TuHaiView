# 最终候选的呈现配置对照

EXE SHA-256：`4507D93F1BD9C484065FA3710E1C89C59B7E7E45A7631ED4526A44597C1225EA`，产品源码 `a504605`。同一 SSD 合成 50,000 张、固定窗口与路线预热，各配置独立五轮；15 轮均有有效日志、结束凭据与 PresentMon 显示样本。系统文件缓存状态未知。

| 配置 | 各轮显示 P95 范围 ms | 各轮呈现延迟中位数约 ms | 显示目标 |
| --- | ---: | ---: | --- |
| [Mailbox](v4-presentation-mailbox.json) | 32.026～32.055 | 66.19～66.39 | 五轮均未通过 |
| [Mailbox + 8 ms 测试轨迹重绘间隔](v4-presentation-mailbox-paced.json) | 31.987～32.036 | 67.75～68.70 | 五轮均未通过 |
| [FIFO](v4-presentation-fifo.json) | 32.069～32.095 | 123.38～123.66 | 五轮均未通过 |

每轮中位数、P95、P99、最大值、GPU 指标、命中证据与原始文件路径见链接的 JSON。15 轮均没有大于 50 ms 或 100 ms 的显示停顿，PresentMon 的 `AllowsTearing` 均为 0；这些记录不替代物理屏幕撕裂观察。

维持默认 Mailbox。FIFO 没有改善显示间隔，呈现延迟明显增加；8 ms 配置也没有足够显示收益。节流参数作用于显式开启的自动轨迹，不能据此声称正常交互的输入延迟改善。未进行新的渲染架构调整或提高图片资源预算。

活动目标报告约 60 Hz，因此仍按 P95 ≤ 17.167 ms、P99 ≤ 33.833 ms 判定，没有用实测约 31 ms 周期放宽门槛。OS 报告强制目标且未检测到连接的显示器；[显示路径诊断](DISPLAY-PATH.md) 保留独立证据和边界。物理显示体验、实际输入到显示延迟仍未验证，当前系统目标上的显示性能未通过。

活动路径元数据采集在三组之间加入：FIFO 的逐轮元数据直接包含该字段，另外两组保留同期独立读取的活动路径；这是记录能力的差异，不能宣称三组均具有同样完整的逐轮路径字段。各组内部五轮的二进制、窗口、DPI、显示配置一致。

原始目录：`F:\tuhai-validation\presentation-v4`。这组测试不替代最终版本的短场景全矩阵或 SSD/HDD 各五轮 30 分钟内存验收。
