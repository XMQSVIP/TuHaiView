# 2026-09-06 验证记录（尚未通过完整验收）

最新原生上传复用候选为 `FAE039A3…`，[MESH-UPLOAD.md](MESH-UPLOAD.md) 记录独立变量对照、补丁及 GPU 正确性证据。此前 `19199B97…` 的 80 轮完整短矩阵作为旧候选对照保留；不把旧二进制测量记为新候选通过。

06:23 新增输入模态计时修正的候选为 `4507D93F1BD9C484065FA3710E1C89C59B7E7E45A7631ED4526A44597C1225EA`，40 项 Release 回归、图片分片 GPU 读回及扩展的多网格 GPU 对照通过。此前 `FAE039A3…` 已有一轮 SSD 30 分钟内存通过记录：斜率 0.1005 MiB／分钟、末段增长 3.9414 MiB；不能转记到新哈希或称为十轮全部完成。图表见 [内存与预算](mesh-reuse-30min-ssd.png)。

06:24 新候选的实际临时权限故障回归通过（`permission-v4.json`）：全新独立测试目录包含两张合成 JPEG 副本，先完成索引，再仅拒绝其中一个子目录的读取；OS 拒绝已确认，目录校验只访问可读的一张，数据库仍保留两条。恢复权限后修改被保护副本，扫描重新访问两张并更新大小。访问规则及继承保护恢复；Windows 为 DACL 增加自动继承标记，SDDL 控制标志不逐字相同，因此后续脚本检查实际规则而非原始 SDDL 文本。原图库及其他目录未改动；该正确性测试不声称完成输入或显示延迟验收。

应用关闭后的独立五轮 DXGI 垂直空白等待也约 31～32 ms，而活动目标信号约 60 Hz；详见 [显示路径诊断](DISPLAY-PATH.md)。不修改刷新率、不把该差异解释成特定驱动根因，也不据此放宽目标。[输入验证边界](INPUT-ATTEMPT.md) 保留工具限制及模态等待测量问题。

06:36 通过官方标志定义确认 `friendlyNameForced=1`：OS 报告强制显示目标、没有检测到连接显示器，且无有效 EDID 标识。当前呈现 CSV 可以作为此系统目标上的测量证据；物理显示器体验仍未验证。脚本现为每轮保存活动显示路径，后续不同显示环境不可混为同一组五轮。

对比起点为 `dc00f1a`。`1d0e71c` 只增加第一版测量和样本工具；`81dc153` 增加视口批量调度、成对发布目录与排序结果、后台回收和 GPU 分片资源租约。

本轮继续修复：过期排序表移交回收线程；GPU 分配未回收时保留像素并让新预览优先；旧预览完成与新请求竞态；取消和结果发布竞态；缓存写失败残留临时文件。没有提高 512/96/32/256 MiB 预算，也没有更换默认 Mailbox。

日志 schema 3 使用单调时钟、QPC、帧/场景/请求关联，退出后生成 `.complete.json` 落盘凭据。报告检查凭据长度、运行 ID、丢失记录、场景结束、最小有效显示样本和最小化状态。`input_frame_processing_ms` 只测 egui 收到输入后的处理，不等于操作系统输入到显示延迟。

`scripts/run_acceptance_matrix.ps1` 提供短场景、三种呈现设置、短内存循环和 SSD/HDD 各五轮 30 分钟矩阵。脚本存在不等于矩阵已经执行，实际状态以逐轮报告为准。每次运行附 EXE/PresentMon/清单哈希、磁盘、DWM 刷新率；系统文件缓存均标记为未知。

固定真实集 50,000 张，23,399,709,977 字节，49,055 JPEG、945 PNG。原图库只做有上限的分类轮流取样，样本不是全库分布估计。跨盘相同 1,333 张、536,573,718 字节；C 盘保留至少 2 GiB。F 和 G 为同一块 HDD。所有真实图片留在本地测试目录，不包含在代码或公开包内。

最终验证 EXE `19199B97…` 的五轮短场景由 [SHORT-MATRIX.md](SHORT-MATRIX.md) 汇总，JSON 保留每轮全部统计与原始记录位置。`scripts/render_matrix_report.py` 只展示已有结果，不将未运行场景补为通过。合成 10k/50k 的 SSD/HDD 组已完成；滚动 P95 仍未通过。测试脚本增加运行契约持久化和后续聚合复核，12 项报告回归通过。

04:57 完成全部 16 组短场景、80 次有效运行。8 个数据/磁盘组合的打开与滚动各五轮，应用管理资源预算全部通过；40 轮滚动可见纹理全命中，但全部未达到 60 Hz P95 目标。无 >50 ms 停顿，显示最大 32.80 ms。产品源码相对 `433cebc` 未变，EXE SHA 一致；后续提交只补工具、示例与测量证据。

`cpu_retired_estimated_bytes`、目录/排序表统计是结构载荷估计，排除共享记录字符串和分配器开销，不能加总当作实际进程内存。解码、像素队列、缓存队列与原生纹理由字节租约独立约束；进程 private bytes 和工作集另外记录。纹理租约直到 wgpu 提交完成回调才归还。

截至修复后的首轮诊断，DWM 报告 60 Hz，实际显示 P95 约 32.04 ms，仍未达到 17.17 ms 目标。修复前后空目录也观察到约 31 ms 周期，提示需要继续检查共同的呈现路径和显示环境；这不是根因证明。FIFO 诊断延迟更高，未据此更换默认配置。存在 ToDesk 进程和虚拟显示驱动，尚未确认当前连接状态。

缺少 Windows 10/11 干净系统环境和系统冷缓存窗口。当前进程无管理员令牌，隔离小容量测试卷的真实写满测试尚未执行。不得把这些缺项或诊断数据标成正式验收通过。

## 追加诊断与已完成原生基准

`native-summary.json` 保存同一 F 盘数据库路径交替五轮的汇总：基线中位数 2.85 s，候选 2.93 s，增加 2.81%。24MP/48MP JPEG 五轮分别减少 77.52%/78.47% 解码时间，48MP 单进程峰值工作集中位数 168.05 → 26.02 MiB。固定真实 1,000 张 JPEG 缓存减少 93.64%。原始 TXT 一并保存；这些基准对应 B77 候选，不能自动归属随后诊断 EXE。

- B77 长诊断 1738.50 s 被磁盘余量保护终止，没有完成凭据，且未获得 PresentMon。分钟 5～28 private bytes 斜率 7.59 MiB/分钟、增长 152.30 MiB，超过阈值。原始日志保存在 F 盘 `long-diagnostic-ssd`，避免把约 1.9 GiB 日志加入仓库。
- `system-heap-probe.json`：E85 EXE，10 分钟正常结束，Rust 活跃分配与 wgpu 分配计数周期性回落；第 4 与第 8 分钟进程内存中位数仍增加 23.89 MiB。
- `mimalloc-probe.json`：D99 EXE，同样 10 分钟，第 4/8 分钟增加 11.29 MiB，且总体占用更高，没有采用为默认分配器。
- `empty-render-memory.json` 与 `empty-no-label-memory.json`：空目录持续绘制仍有增长，去除底层调试标签没有消除；不是图片集合或缩略图队列累计的充分证据，也不证明特定原生库泄漏。
- 03:01 后加入仅限测试期间的显示唤醒请求，重新取得 PresentMon CSV。空目录、8 ms 节流、16 ms 节流和进程 1 ms 定时器诊断的实际显示仍约 31 ms。没有将它们写成满足 60 Hz 显示目标；普通配置没有改变。

诊断文件的 SHA 是日志校验和；EXE SHA 和启动参数在对应 F 盘 `*-run.json`。`heap-diagnostics` 与 `mimalloc` 构建用于定位，不能替代最终产品二进制的验收。
