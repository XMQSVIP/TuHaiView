# 2026-09-06 验证记录（尚未通过完整验收）

对比起点为 `dc00f1a`。`1d0e71c` 只增加第一版测量和样本工具；`81dc153` 增加视口批量调度、成对发布目录与排序结果、后台回收和 GPU 分片资源租约。

本轮继续修复：过期排序表移交回收线程；GPU 分配未回收时保留像素并让新预览优先；旧预览完成与新请求竞态；取消和结果发布竞态；缓存写失败残留临时文件。没有提高 512/96/32/256 MiB 预算，也没有更换默认 Mailbox。

日志 schema 3 使用单调时钟、QPC、帧/场景/请求关联，退出后生成 `.complete.json` 落盘凭据。报告检查凭据长度、运行 ID、丢失记录、场景结束、最小有效显示样本和最小化状态。`input_frame_processing_ms` 只测 egui 收到输入后的处理，不等于操作系统输入到显示延迟。

`scripts/run_acceptance_matrix.ps1` 提供短场景、三种呈现设置、短内存循环和 SSD/HDD 各五轮 30 分钟矩阵。脚本存在不等于矩阵已经执行，实际状态以逐轮报告为准。每次运行附 EXE/PresentMon/清单哈希、磁盘、DWM 刷新率；系统文件缓存均标记为未知。

固定真实集 50,000 张，23,399,709,977 字节，49,055 JPEG、945 PNG。原图库只做有上限的分类轮流取样，样本不是全库分布估计。跨盘相同 1,333 张、536,573,718 字节；C 盘保留至少 2 GiB。F 和 G 为同一块 HDD。所有真实图片留在本地测试目录，不包含在代码或公开包内。

`cpu_retired_estimated_bytes`、目录/排序表统计是结构载荷估计，排除共享记录字符串和分配器开销，不能加总当作实际进程内存。解码、像素队列、缓存队列与原生纹理由字节租约独立约束；进程 private bytes 和工作集另外记录。纹理租约直到 wgpu 提交完成回调才归还。

截至修复后的首轮诊断，DWM 报告 60 Hz，实际显示 P95 约 32.04 ms，仍未达到 17.17 ms 目标。修复前后空目录也观察到约 31 ms 周期，提示需要继续检查共同的呈现路径和显示环境；这不是根因证明。FIFO 诊断延迟更高，未据此更换默认配置。存在 ToDesk 进程和虚拟显示驱动，尚未确认当前连接状态。

缺少 Windows 10/11 干净系统环境和系统冷缓存窗口。当前进程无管理员令牌，隔离小容量测试卷的真实写满测试尚未执行。不得把这些缺项或诊断数据标成正式验收通过。
