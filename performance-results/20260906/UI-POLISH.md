# UI 保守打磨验证（2026-09-06）

## 后续扫描顺序修复

扫描期间图片反复换位已另行修复：新图片追加到末尾，结束后统一排序一次。最新构建哈希、56 项常规回归、DX12 分批位置回归和 50k 搜索数据见 [扫描顺序修复报告](SCAN-ORDER.md)。下文的各次 UI 构建哈希和性能数据属于历史记录；其中“未修改排序协议”“未更新版本号”等说明仅适用于当时的保守打磨阶段。

## 20260906 版本追加修正

- 按用户反馈取消状态及扫描进度的 360 像素截断，改为完整文本自然换行；路径仍限宽并可悬停查看全文。
- 按用户选择，将大图画布改为中性浅灰 `#E8E8E8`，工具栏跟随主界面主题，错误文字使用主题错误色。[预览渲染截图](ui-polish/preview-light.png)。
- 卡片布局边框固定为 1 像素，选中边框另在内部绘制；滚动条固定占位，避免显隐改变列宽。实际复选框取消/勾选测试比较所有可见文件名矩形，坐标不变；菜单切换、预览翻页与筛选后复制回归通过。[测试输出](ui-polish/selection-regression.txt)、[完整状态及选中网格](ui-polish/full-status-stable-selection.png)。
- 常规 Release 测试 54 通过、8 默认忽略；UI/DX12 专项单独运行通过。后续只补充了等价的 `f32` 字面量类型标注，编译检查无警告。上述图为真实应用的离屏渲染，未重做 Windows DPI/物理呈现验收。
- 用户已授权调整版本：标题、关于、README 为 `20260906`，Windows 版本资源为 `2026.9.6.0`。未发布或推送。下面早期构建的版本说明、哈希、性能数据保留为历史记录。

后续修正：欢迎页整体上移 28 逻辑像素；补齐“关于”和排序/缓存等工具栏入口间的互斥切换，切换会取消未确认弹窗，后台结果不再抢回已关闭界面。最终源码 Release 常规测试仍为 54 通过、8 默认忽略；单独执行真实控件点击回归通过，见 [菜单回归输出](ui-polish/menu-regression.txt)。该次截图位于 `G:\tuhai-ui-qa-menu-1c399bde0a44438ca3c61a0d712815e7\captures`。下方哈希与原生窗口性能对照属于这次后续修正之前的构建，保留为历史证据，不转记为新构建性能结论。

保留全部工具栏入口、顺序及卡片结构。只调整长文本适配、控件整体换行、文件名字号、选中样式与空状态居中；版本号不变。

## 功能与视觉

- Release 常规测试：54 通过，8 默认忽略；新增的忽略项是显式运行的 UI/DX12 验收工具。
- 独立运行缓存清理回归通过：无目录反复清理、打开目录后清理、旧排序竞争、空目录以及保留搜索条件。[输出](ui-polish/clear-regression.txt)。
- 独立运行 `app::ui_qa::layout_interaction_and_search_50k` 通过：[输出](ui-polish/ui-qa.txt)。覆盖 37 张临时图片、全选后筛选仅保留 10 张、复制恰好 10 个文件、预览前后翻页、退出预览保留搜索、无结果清除选择及清空不恢复选择。
- 中文 IME 的预编辑/提交和 Ctrl+F、Ctrl+A、Delete、Enter、Esc 由既有 egui 事件测试覆盖。本轮未完成真实 Windows 输入法候选窗验收。
- DX12 渲染了 900×600、1280×820、1920×1080 三种逻辑尺寸，每种 1.0/1.25/1.5 像素密度的欢迎页与选中网格，另有普通网格、筛选等待、空结果和空目录。窗口大小或字体图集变化后先运行四帧让布局稳定，再取图。
- 人工查看截图发现并修正了排序控件横向越界、空结果按钮竖排。最小尺寸下全部入口和搜索行可见；选中状态、等高卡片、居中空状态已查看。

代表截图：[欢迎页](ui-polish/welcome-1280-100.png)、[900×600 选择与批量栏](ui-polish/selected-900-100.png)、[网格](ui-polish/grid.png)、[筛选等待](ui-polish/search-pending.png)、[无结果及隐藏副本提示](ui-polish/no-match-hidden.png)、[空目录](ui-polish/empty.png)。

完整 23 张图及生成的临时副本位于 `G:\tuhai-ui-qa-50fddec305de4ec389b87c7079d911c3`。测试使用实际 App::update、字体和 DX12 绘制，不是设计稿；不包含 Windows 窗框或屏幕呈现。桌面截图接口两次返回 `SetIsBorderRequired ... 0x80004002`，因此真实系统 DPI 切换、最大化窗口及输入法候选窗没有作为“已通过”记入。

## 50k 搜索与滚动

相同后台排序基准，每类 20 次，包含 150 ms 防抖：

| 匹配量 | 修改前 P95 | 修改后 P95 |
| --- | ---: | ---: |
| 大量 | 178.83 ms | 196.79 ms |
| 少量 | 159.85 ms | 164.85 ms |
| 零结果 | 159.99 ms | 166.34 ms |

后台搜索代码未变，上述单轮差异不作为性能改善或退化结论。全部低于 300 ms，且不含 UI 调度和屏幕呈现。

最终 UI 集成基准采用 50,000 条共享记录、自然排序、真实搜索输入控件和后台发布；每类 20 次，以 16 ms 间隔驱动测试帧，计时至对应结果被 App::update 应用。

| 匹配量 | P50 | P95 | 最大值 |
| --- | ---: | ---: | ---: |
| 大量 | 190.16 ms | 209.70 ms | 212.79 ms |
| 少量 | 169.36 ms | 171.47 ms | 183.62 ms |
| 零结果 | 169.42 ms | 185.75 ms | 185.97 ms |

测试帧的滚动更新（40 帧预热，200 帧计入）P50 0.34 ms、P95 0.75 ms、最大 1.10 ms，仅包含 UI 更新和纹理增量处理。50k 记录共享测试图片以控制 I/O；此结果不代表 50k 独立图片的解码成本、原生窗口帧率或真实输入到显示延迟。

随后分别运行修改前/后真实 EXE，各 50 秒，使用现有 `C:\tuhai-fixtures-20260905\catalog` 的 50,000 个独立测试图片、相同自动滚动轨迹和独立应用缓存。两版正常退出，凭据核对 accepted=written、dropped=0、日志字节数相符。滚动段可见纹理缺失均为 0。[原始日志位置及指标](ui-polish/native-scroll.json)。

| 原生窗口滚动阶段 | 修改前 P95 | 修改后 P95 |
| --- | ---: | ---: |
| UI update | 1.642 ms | 1.656 ms |
| 应用 frame interval | 15.760 ms | 18.184 ms |
| CPU render frame | 13.534 ms | 15.470 ms |

这是一次短对照，系统负载及文件缓存未隔离，采样阶段长度也受扫描完成时间影响。UI 更新接近，帧间隔和渲染阶段略高，不据此宣称整体提速或性能验收完成。本次没有使用 PresentMon；以上 frame interval 不是物理显示间隔。

通过应用自带 framebuffer 捕获查看了最终 EXE 的真实窗口：[滚动截图](ui-polish/native-scroll.png)。这补充了离屏截图，但不替代真实系统 DPI 矩阵、操作系统输入法或物理显示测试。

最终 EXE：`G:\tuhai-perf-build\release\TuHaiView.exe`，SHA-256 `415358F180EFDA2844A29F2546A670CCD5975581D9923F136B06574CAA9ABFEF`。修改前对照 EXE SHA-256 `FADE5E235E9D4BB7FC4EAC01208A183C4248BD210B683ABCFAAFE07C7DD7EA0C`。测试 EXE 包含验收工具，哈希不与产品 EXE 混用。

## 复现与限制

测试工具和隔离运行说明见 [DEVELOPMENT.md](../../DEVELOPMENT.md)。缓存清理使用专门的新建 `tuhai-clear-regression-*` 目录；UI 验收使用新建 `tuhai-ui-qa-*` 目录。不要将测试 EXE 放入产品数据目录运行。

既有长期性能验收和物理屏幕呈现问题不属于本轮已解决事项。没有更改排序/搜索协议、数据库、缓存预算或后台释放机制；没有发布、推送或更新版本号。
